import ChakraCarousel from '../carousel'

export default function ReadingNowCarousel(props) {
    const readingNowBooks = props.map((book.bookStatus=='')=>{<BookCard})
  return <ChakraCarousel></ChakraCarousel>
}
